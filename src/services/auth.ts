/**
 * Authentication Service
 * Currently mocked for Milestone 5.
 * Will be replaced with real JWT/OAuth logic in future milestones.
 */
export function isAuthenticated(): boolean {
  return true;
}
